const {
    getAllTasksController,
    createTaskController,
    pageViewController,
    deleteTaskController,
} = require('./taskController');

module.exports = {
    getAllTasksController,
    createTaskController,
    pageViewController,
    deleteTaskController,
};
